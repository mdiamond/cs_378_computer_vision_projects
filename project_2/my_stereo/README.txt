Use our create_mosaic.py file to create the disparity image and the ply file like so:

python create_mosaic.py {left image} {right image} {output PLY filename}

disparity_image.png will be written into the directory the script was run in, and the PLY file will be written where you choose.
